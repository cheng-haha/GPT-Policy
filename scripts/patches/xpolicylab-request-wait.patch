diff --git a/client_server/ws/model_client.py b/client_server/ws/model_client.py
index 05dd997..8d07031 100644
--- a/client_server/ws/model_client.py
+++ b/client_server/ws/model_client.py
@@ -40,6 +40,7 @@ class WsModelClient:
         connect_timeout_s: float | None = None,
         handshake_timeout_s: float | None = None,
         request_timeout_s: float | None = None,
+        request_warning_s: float | None = None,
         max_connect_attempts: int | None = None,
         connect_retry_delay_s: float | None = None,
         max_connect_seconds: float | None = None,
@@ -56,6 +57,7 @@ class WsModelClient:
             "connect_timeout_s": connect_timeout_s,
             "handshake_timeout_s": handshake_timeout_s,
             "request_timeout_s": request_timeout_s,
+            "request_warning_s": request_warning_s,
             "max_connect_attempts": max_connect_attempts,
             "connect_retry_delay_s": connect_retry_delay_s,
             "max_connect_seconds": max_connect_seconds,
diff --git a/client_server/ws/protocol/client.py b/client_server/ws/protocol/client.py
index c2bcc16..b7d42bb 100644
--- a/client_server/ws/protocol/client.py
+++ b/client_server/ws/protocol/client.py
@@ -88,12 +88,13 @@ class PolicyEvalClientConfig:
     url: str
     evaluation_id: str
     connect_timeout_s: float = 30.0
-    request_timeout_s: float = 120.0
-    # Handshake budget, separate from request_timeout_s: the server answers
-    # HELLO without touching the model (it is constructed before the port
-    # opens), so this is a plain round-trip and must not inherit the timeout
-    # sized for slow inference — otherwise a hung server stalls every connect
-    # attempt for minutes. Raise it for servers that do work on HELLO.
+    # Model calls are unbounded.  A slow multimodal inference must not be
+    # mistaken for a dead websocket; request_warning_s below reports slow
+    # calls without cancelling them.  This legacy field is retained for
+    # configuration compatibility and is used only as a warning fallback.
+    request_timeout_s: float | None = None
+    # Warning threshold for the HELLO handshake. It is not a hard deadline;
+    # once a socket is connected, a slow server is allowed to finish.
     handshake_timeout_s: float = 60.0
     # Cold-start budget: a policy server loading a large checkpoint has not
     # opened its port yet, so the client sees connection refused and retries.
@@ -112,6 +113,7 @@ class PolicyEvalClientConfig:
     ws_ping_interval_s: float | None = 20.0
     ws_ping_timeout_s: float | None = 20.0
     proxy: str | Literal[True] | None = None
+    request_warning_s: float | None = 120.0
 
     def __post_init__(self) -> None:
         self.ws_ping_interval_s = normalize_ws_ping(
@@ -125,7 +127,6 @@ class PolicyEvalClientConfig:
         # documented "no wall-clock cap", so 0 stays allowed everywhere).
         for name in (
             "connect_timeout_s",
-            "request_timeout_s",
             "handshake_timeout_s",
             "connect_retry_delay_s",
             "max_connect_seconds",
@@ -141,6 +142,17 @@ class PolicyEvalClientConfig:
                 raise ValueError(
                     f"{name} must be a non-negative finite number, got {value!r}"
                 )
+        for name in ("request_timeout_s", "request_warning_s"):
+            value = getattr(self, name)
+            if value is not None and (
+                isinstance(value, bool)
+                or not isinstance(value, (int, float))
+                or not math.isfinite(value)
+                or value < 0
+            ):
+                raise ValueError(
+                    f"{name} must be None or a non-negative finite number, got {value!r}"
+                )
         if self.max_connect_attempts < 1:
             raise ValueError(
                 f"max_connect_attempts must be >= 1, got {self.max_connect_attempts!r}"
@@ -518,16 +530,34 @@ class PolicyEvalClient:
                 )
             raise
         timeout = timeout_s if timeout_s is not None else self.config.request_timeout_s
+        warning = self.config.request_warning_s
         try:
-            response = await asyncio.wait_for(fut, timeout=timeout)
-        except asyncio.TimeoutError as exc:
-            self._pending.pop(request_id, None)
-            # The server may STILL be executing this request. Dedup only
-            # covers retries with the SAME request_id, so callers must treat
-            # a timeout as fatal for the trial — re-issuing the logical call
-            # under a fresh request_id would execute it a second time
-            # (update_obs/get_action are not idempotent for stateful models).
-            raise WsError(ErrorCode.TIMEOUT, f"timeout waiting for {expected}") from exc
+            # Every request, including HELLO, is unbounded after the socket
+            # connects. Use the request/handshake setting as a warning
+            # threshold only, then keep waiting for the protocol response.
+            warning_threshold = (
+                timeout
+                if msg_type == MessageType.HELLO
+                else (warning if warning is not None else timeout)
+            )
+            if warning_threshold is None:
+                response = await fut
+            else:
+                # Shield the protocol future: reaching the warning threshold
+                # must not cancel the request that the policy server may still
+                # be executing.
+                try:
+                    response = await asyncio.wait_for(
+                        asyncio.shield(fut), timeout=warning_threshold
+                    )
+                except asyncio.TimeoutError:
+                    warning_message = (
+                        f"{msg_type.value} request {request_id} has been waiting "
+                        f"for at least {warning_threshold:g}s; continuing to wait"
+                    )
+                    logger.warning(warning_message)
+                    _status("WARNING", _YELLOW, warning_message)
+                    response = await fut
         except asyncio.CancelledError:
             # Outer cancellation (e.g. KeyboardInterrupt around
             # run_until_complete): wait_for cancels fut, but the _pending
diff --git a/utils/debug_env_client.py b/utils/debug_env_client.py
index 7d7bc7d..89cec18 100644
--- a/utils/debug_env_client.py
+++ b/utils/debug_env_client.py
@@ -45,6 +45,7 @@ class TestEnv:
                 connect_timeout_s=deploy_cfg.get("connect_timeout_s"),
                 handshake_timeout_s=deploy_cfg.get("handshake_timeout_s"),
                 request_timeout_s=deploy_cfg.get("request_timeout_s"),
+                request_warning_s=deploy_cfg.get("request_warning_s"),
                 max_connect_attempts=deploy_cfg.get("max_connect_attempts"),
                 connect_retry_delay_s=deploy_cfg.get("connect_retry_delay_s"),
                 max_connect_seconds=deploy_cfg.get("max_connect_seconds"),
